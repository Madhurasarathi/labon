VM ware

file > new vm, custom - next > next
use ISO image - Browse path TrueNAS-13.3-UI.2.iso - next > next
memory 2gb^ > NAT > Recommended(LSI) > SCSI > create new virtual disk 
> *Allocate all disk space now *store virtual disk as a single file
>next > next >finish 
>yes >yes, space + enter, yes, enter
set password root - root OK
Boot via bios - reboot the system, ok (ctrl+alt) > shutdown 

edit vm setting (add 2)
+ add hard disk > scsi > create virtual disk>
disk size-20GB *allocate all disk space now *store virtual disk as single file
>next finish > apply and save

run again

LOGIN through ip
(if required same for webdav)
STORAGE - pool +add create pool
nmae: pool *show disk with non unique serial number
select dd1 --> *force confirm> continue> create> confirm> create pool

SHAIRNG - UNIX NFS, path -pool, *all dir
advanced options maprort user- root, group- wheel
submit> enable service> close

SERVICES- nfs- edit *nfs4
other- allow non root mount- save

IN TERMINAL- cd /mnt/
sudo mkdir nfs
sudo mount -t nfs -o nfsvers=4 <IP>:/mnt/pool /mnt/nfs/

SHARING - windows shares[SMB], ADD+ /mnt/pool(path)
advanvced options- *allow guest access- submit, enable service, close

ACCOUNTS - users> add> n:winduser, username:winduser, P:root, root
*home directory permission [to all]

SERVICES- SMB edit *enable SMB1 support1
advanced options> guest account -> winduser >save
 
GOTO WINDOWS VM> network right click-> map network device
folder(\\\<IP>\pool *connect using credentials
username:winduser, p:root
